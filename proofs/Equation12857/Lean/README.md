# E12857 Lean 形式化工作区

状态：**证明草稿，尚未通过 Lean 编译；不能据此标记形式化完成。**

已编写：树与重写关系、终止性、逐规则的所有根部/上下文重叠证明、局部汇合、规范形唯一性、原等式、自然数到模型的单射和对偶等式。

`Peak1.lean` 到 `Peak10.lean` 由 `scripts/generate_12857_lean.py` 生成。它们是供 Lean 内核检查的普通证明文本，不把 Python 检查结果作为公理。

初次编译触及 256 MiB 的 Lean 内存上限。最小树声明在现有工具链中也触及该限制，尚未开始较高上限的验证。已向用户请求许可：串行编译，Lean 上限 768 MiB，监控实际进程 RSS，超过 1 GiB 即停止。

获准后运行：

```sh
python3 scripts/check_12857_lean.py
```

该命令逐文件编译、保存日志和哈希，任何失败都会停止。`Audit.lean` 检查最终定理的类型和依赖公理。正式验收必须等待所有模块编译成功，且审计输出不含 `sorryAx` 或自定义公理；当前未达到该条件。
