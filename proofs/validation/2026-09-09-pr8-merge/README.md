# PR #8 合并核对

本次将 main `4a63e32fe57c108e634de4bdf1c3a549dfa4361e` 合并到 PR 分支 `171db55cdb592645fcdaec1365dcd3a6feeb36cf`。按仓库维护者确认，`ZplusM` 为嘉铭；冲突优先保留嘉铭提交的版本。

- 107 个冲突文件：60 个 README、24 个 InfiniteModel.lean、22 个 JudgeProblem.lean、1 个 index.json。
- 冲突文件先采用 main 版本，再在索引和文档中补充本分支已有的有限侧记录及用户要求的状态标签、缺口统计。
- main 已存在的 Lean 文件全部保持字节不变。嘉铭提交涉及的 120 个现存非 Markdown、非总索引文件也逐一核对，均保持 main 字节不变。
- 模型、补充模型、显式无限性、Aurora 验收和无有限性假设的平凡性证据保留 main 的记录；合入本分支已提交的有限侧证书记录。

当前提交库存：114 份有限侧证书、98 份非平凡模型证书。有限侧按表 20.1 / 20.2 / 20.3 为 10 / 96 / 8，缺口为 0 / 0 / 16；模型侧缺口为 0 / 10 / 22，其中 2 条已由嘉铭的 Triviality.lean 证明不存在非平凡模型，不属于待补证明。

此前 README 使用了含未提交文件的本地库存，写为 120 份有限侧证书。本次统计改为已提交的 114 份，不纳入另外 6 份本地证书。嘉铭的 Triviality.lean 保留为两条排除结论的链接目标。

核对内容：130 条方程及对偶关系、证书与依赖 SHA-256、本地编译记录的输入哈希、56 份 Aurora accepted 回执及任务编号、根 README 和 130 个方程 README 的本地链接、生成表格与索引一致性。README 文档哈希随内容更新；未更改已验收证书的哈希以绕过验证。旧索引引用但两边提交均未包含的一份构造文档，移入 inventory_reconciliation.unavailable_prior_references 保留历史信息。

本次未运行 Lean 编译或云端 Judge，不产生新的数学验证结论。只核对已归档证据与当前文件的一致性。

复核：`python3 scripts/build_index.py --check`。脚本使用标准库，逐文件分块计算哈希，不启动求解器。

本次新编辑的 README、索引和生成脚本通过空白检查。完整合并差异中的空白提示来自 main 既有证书末尾空行及回执 CRLF；为保持验收哈希和嘉铭原件，不改写这些文件。
